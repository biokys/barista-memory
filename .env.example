# Copy to .env and fill in. scripts/archive.sh sources it if present.
GAGGIMATE_PI_HOST=user@raspberrypi.local     # ssh target running the daemon
GAGGIMATE_PI_DIR=/home/user/barista-memory   # where it is deployed there
GAGGIMATE_HOST=192.168.1.50                  # the espresso machine; an IP, not .local (mDNS is slow)
